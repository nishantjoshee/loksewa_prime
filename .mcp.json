{
  "mcpServers": {
    "github": {
      "command": "bash",
      "args": [
        "-c",
        "GITHUB_PERSONAL_ACCESS_TOKEN=$(gh auth token 2>/dev/null || echo \"$GITHUB_TOKEN\") npx -y @modelcontextprotocol/server-github"
      ]
    }
  }
}
